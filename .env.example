# GoHighLevel (GHL) inbound webhook URL.
#
# Client-side (preferred for GHL — posts the lead straight to GHL from the browser):
NEXT_PUBLIC_GHL_WEBHOOK_URL=

# Server-side fallback used by /api/leads if the public var is not set.
# Leave blank to just log leads to the server console during development.
GHL_WEBHOOK_URL=
